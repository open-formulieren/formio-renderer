/**
 * The semantic names of icons that we use. Each icon library alternative should provide
 * an actual icon for each union member.
 */
export type RendererIcon = 'add' | 'edit' | 'remove' | 'tooltip' | 'close' | 'warning' | 'calendar';
