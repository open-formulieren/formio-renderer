import {ArgsTable, Canvas, Meta, Story} from '@storybook/addon-docs';

import {Component} from './component.component';

<Meta title="Components / Reusable / Component" component={Component} />

export const Template = args => (
  <Component {...args}>The quick brown fox jumps over the lazy dog.</Component>
);

# Component

Structural wrapper for the component. A component's implementation should be placed in `children`
prop.

<Canvas>
  <Story name="Component" args={{component: {}}}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Component} />
