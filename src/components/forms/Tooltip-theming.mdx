import ofTokens from '@open-formulieren/design-tokens/dist/tokens';
import {Meta} from '@storybook/addon-docs/blocks';
import TokensTable from 'design-token-editor/lib/esm/TokensTable';

<Meta title="Internal API / Forms / Tooltip / Theming" />

# Tooltip theming

The `Tooltip` component appearance can be tweaked with design tokens.

<TokensTable container={ofTokens} limitTo="of.tooltip" autoExpand />
