import {ArgTypes, Canvas, Meta} from '@storybook/addon-docs/blocks';

import TimeField from '../TimeField';
import * as Stories from './TimeField.stories';

<Meta of={Stories} />

# TimeField - input group

A component to input time related data.

<Canvas of={Stories.Input} />

## Props

<ArgTypes of={Stories} />

## Validation errors

<Canvas of={Stories.InputWithInitialValue} />

## Leading zeros

Leading zeros are not required as the component will add these whenever they are absent.

<Canvas of={Stories.WithoutLeadingZero} />
