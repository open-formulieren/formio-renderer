import {ArgsTable, Canvas, Meta, Story} from '@storybook/addon-docs';
import {Errors} from "./errors.component";

<Meta title="Components / Reusable / Errors" component={Errors} />

export const Template = (args) => <Errors {...args} />

Errors
===

Not implemented.

<Canvas>
  <Story name="Errors" args={{component: {id: 'foo'}}}>
    {Template.bind({})}
  </Story>
</Canvas>


<ArgsTable of={Errors}/>
